# 变量必须以 VITE_ 为前缀才能暴露给外部读取
NODE_ENV = 'development'
# 是否去除console 和 debugger
VITE_DELETE_CONSOLE = true
# 是否开启sourcemap
VITE_SHOW_SOURCEMAP = false

# Production H5 is served by manager-web nginx under /tbot-mobile/.
VITE_APP_PUBLIC_BASE = /tbot-mobile/
VITE_SERVER_BASEURL = '/tbot'
