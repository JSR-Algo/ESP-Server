# P0-cp8-alarm - Verification Report

**Date:** 2026-06-17
**Task type:** AD-HOC
**Owning repo:** `robot/esp32-server/main/tbot-server`

## Task-Start

**Task ID:** P0-cp8-alarm

**Goal:** Feed every completed Google Live voice turn's first-audio-out latency into `ConnectionHandler.note_voice_round_trip`, so `PreloadVoiceLatencyAlarm` can auto-disable lesson runtime on p95 regression during active preload.

**Files in scope:**

- `core/voice/google_live/audio_bridge.py` - Live event lifecycle and first model audio latency.
- `core/connection.py` - alarm feed, lesson-runtime disable callback, bounded voice metric sink.
- `core/lesson/preload_voice_alarm.py` - p95 threshold semantics and disable log.
- `core/lesson/runtime.py` - active preload window bracketing and alarm construction.
- `tests/test_google_live_event_mapping.py` - Live event regression coverage for alarm and metric feed.
- `tests/test_lesson_preload_voice_alarm.py` - alarm threshold/window/latch tests.
- `tests/test_lesson_voice_nonregression.py` - connection-level auto-disable tests.
- `tests/test_connection_voice_provider_routing.py` - shared import-stub isolation used by the lesson voice tests.

**Acceptance criteria:**

- AC1: `note_voice_round_trip(latency_ms)` has a non-test production caller from the Google Live turn lifecycle using already-computed first-audio-out latency.
- AC2: p95 over the preload window above threshold latches `LESSON_RUNTIME_ENABLED` false and records a log/event for the trip.
- AC3: the same latency is exported as a metric or local metric sink while MP-14 exporter is not present.

**Verification plan:**

- Run targeted pytest covering Live event mapping, preload alarm threshold behavior, and lesson voice non-regression.
- Run syntax checks over changed runtime/test files.
- Run caller grep for `note_voice_round_trip(` and confirm a non-test caller exists under `core/`.
- Run the owner test for the shared routing stub after fixing test pollution.

## Read-Before-Code

Read first:

- `docs/system-design/production-unified-runtime.md` section 5.2 step 6 and section 9 from workspace root.
- `core/lesson/preload_voice_alarm.py`.
- `core/voice/google_live/audio_bridge.py`.
- `core/voice/session_provider/google_live.py`.

Reality checks:

- The target repo does not contain a local `.agent/` directory. The task brief's named gate sequence was followed and this evidence artifact records it.
- The current worktree already contained most CP-8 implementation before this continuation: `audio_bridge.py` calls `_record_turn_first_audio_latency()` on first audio chunks, then forwards to `note_voice_round_trip()` and the metric sink.
- `PreloadVoiceLatencyAlarm` still keeps the existing threshold/window semantics; no threshold changes were made.
- The first targeted test run exposed a test-isolation failure: `tests/test_lesson_voice_nonregression.py` imports `tests/test_connection_voice_provider_routing.py`, whose import stubs replaced `core.utils.textUtils` with an empty module during collection. That broke `audio_bridge._derive_emotion()` when the combined targeted suite loaded. The stub now preserves the real `core.utils.textUtils` when available.

## Evidence

Commands run from `/Users/manhhodinh/Documents/TBOT/robot/esp32-server/main/tbot-server`:

```bash
python3 -m pytest tests/test_google_live_event_mapping.py tests/test_lesson_preload_voice_alarm.py tests/test_lesson_voice_nonregression.py -q
```

Result: PASS - 55 passed, 1 warning.

```bash
python3 -m pytest tests/test_connection_voice_provider_routing.py -q
```

Result: PASS - 14 passed, 1 warning.

```bash
python3 -m py_compile core/voice/google_live/audio_bridge.py core/connection.py core/lesson/preload_voice_alarm.py core/lesson/runtime.py tests/test_connection_voice_provider_routing.py
```

Result: PASS - exit 0.

```bash
rg -n "note_voice_round_trip\(" core tests
```

Result: PASS - production caller exists at `core/voice/google_live/audio_bridge.py:976`; connection method exists at `core/connection.py:1617`; remaining hits are tests or the bridge helper.

Key current-state evidence:

- AC1: `core/voice/google_live/audio_bridge.py:261` records turn first-audio latency on the first audio chunk; `:954-969` computes latency from `conn.google_live_turn_started_at`, clears it for one-shot behavior, logs `turn_latency_ms`, calls `_note_voice_round_trip(latency_ms)`, and records the metric.
- AC2: `core/lesson/preload_voice_alarm.py:180` logs `AUTO-DISABLE LESSON_RUNTIME_ENABLED...`; `core/connection.py:1592-1613` flips `config["lesson"]["runtime_enabled"]` false and logs `LESSON_RUNTIME_ENABLED auto-disabled...`.
- AC3: `core/connection.py:1636-1654` records bounded local voice metric samples and logs `voice_metric`; `core/voice/google_live/audio_bridge.py:980-988` writes `turn_latency_ms` with `source=google_live` and `phase=first_audio_out`.
- Test coverage: `tests/test_google_live_event_mapping.py:776-797` verifies each turn's first audio feeds the alarm and metric once; `tests/test_lesson_voice_nonregression.py` verifies the alarm flips the lesson runtime flag; `tests/test_lesson_preload_voice_alarm.py` verifies above/below threshold behavior and latch semantics.

## Critique-Before-Close

- Root cause closed: the alarm had no production feed; current code feeds the already-computed first-audio-out turn latency from the Google Live audio bridge.
- Hot-path safety: `note_voice_round_trip` and `record_voice_metric` are best-effort and exception-swallowing; no alarm or metric failure blocks audio delivery.
- Threshold semantics: unchanged; samples only count while `preload_active` is true and p95 uses the existing nearest-rank rule.
- Remaining risk: this validates targeted runtime behavior, not a full repo suite. The worktree has substantial pre-existing dirty changes across many unrelated files, so full-suite failures could conflate unrelated work.
- Stop condition: all CP-8 ACs have current-state evidence and targeted verification passed.
