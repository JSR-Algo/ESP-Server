plugins:
  get_weather:
    # Open-Meteo backend is keyless — no api_key required. default_location is
    # used only when no city is named and the device IP cannot be geolocated.
    default_location: "Ho Chi Minh City"
  # Add other plugin configs here
