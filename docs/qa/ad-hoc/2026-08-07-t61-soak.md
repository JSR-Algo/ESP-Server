# T6.1 — Soak & memory (long lesson sessions)

**Status: BLOCKED — NOT DONE. No soak was run and no scope file was modified.** The task's
dependency gate (`Depends on: T5.3 DONE`) fails, and the failure is substantive, not clerical:
three of the five deep-dive boxes are unreachable by construction while T5.3 is blocked.

- **Task file:** `lesson-prod/t61-soak.md`
- **Branch:** none created (see "Ship checklist — N/A" below)
- **Dependency:** T5.3 is **BLOCKED** in `LESSON_PRODUCTION_PLAN.md` §2, not DONE.
- **Scope files (untouched):** `robot/docs/evidence/lesson-memory-soak.md`,
  `robot/scripts/lesson_studio_task14_soak.py`

## Gate check — what was verified

### 1. Dependency is BLOCKED, and the blocker guts this task's core

`LESSON_PRODUCTION_PLAN.md` §2 records T5.3 as BLOCKED on **F-T53-09**: no generation index is
buildable from the lesson-studio fixture (one demo-scoped lesson cannot be both the required
curriculum lesson and the required demo), so `lesson_asset_generations` stays empty and every
`POST /v1/admin/lesson-assignments` returns 409 `ASSET_PACK_NOT_READY`. T5.3's own evidence
records **0 checkpoints past `lesson_start_requested`**.

T6.1's first deep-dive box is "N≥20 consecutive lessons". The stack cannot start one. Box-by-box
reachability under the current blocker:

| # | Deep-dive box | Reachable? | Why |
| --- | --- | --- | --- |
| 1 | N≥20 consecutive lessons: ESP RSS/fd/task flat | **No** | assign 409s; no lesson ever starts (F-T53-09) |
| 2 | M-hour idle after lessons: no creep | **No** | precondition is "after lessons"; there are none |
| 3 | WS churn soak (connect/disconnect loop) | **Yes** | device connects since F-T53-01 cleared; needs no lesson |
| 4 | Backend generation+ingest sustained load | **Partial** | ingest loadable; generation blocked by F-T53-09 |
| 5 | SD pack materialize/evict cycling | **No** | a pack only exists downstream of an assignment |

Running only boxes 3 and the ingest half of 4 would produce a "no monotonic growth" claim about a
soak window in which no lesson ever ran — a Done-criteria claim the run would not support. Per
operator decision, T6.1 is parked as BLOCKED rather than run as a partial.

### 2. Verify command passes, but is not evidence of a soak

```
$ cd robot && python3 scripts/lesson_studio_task14_soak.py --help
usage: lesson_studio_task14_soak.py [-h] [--minimum-transitions ...] [--self-test] [logs ...]
Parse Task 14 serial/server logs without contacting a robot or production.
```

Exit 0. **But the named base tool is a log *parser*, not a soak driver** — it consumes
already-captured serial/server logs and asserts transition/SRAM thresholds over them. The plan
(§ "T6.1 — Soak & memory") calls it "a base", so building the actual N-lesson/M-hour driver is
in-scope T6.1 work that does not yet exist. Consequence for whoever picks this up: **T6.1's
`Verify commands` block is green whether or not any soak has ever been run**, so it must not be
treated as the Done gate. The Done gate is the five deep-dive boxes.

Note `python3` is required — there is no `python` on PATH on this host.

### 3. Ship checklist — N/A, and why

Both scope files live in `robot/`, which is **not a git repository**:

```
$ git -C /Users/manhhodinh/Documents/TBOT/robot rev-parse --show-toplevel
fatal: not a git repository (or any of the parent directories): .git
```

(`robot/esp32-server`, `tbot-backend`, `tbot-mobile` are each real repos; `robot/` itself is not.)
Branch `lesson-prod/t61-soak` exists in no repo. Ship steps **2 (merge to main via the gate)** and
**5 (remove the worktree / confirm branch merged)** therefore have no object to act on for this
task's named scope files. This is the already-open **F-T53-03**, not a new finding. Per operator
decision, any future T6.1 work is to be written in place under `robot/` with ship recorded N/A.

**Precedent worth following, recorded after a mid-session re-check.** F-T53-03 was originally
filed as "the entire E2E verification harness is untracked"; that wording is now partly
superseded. T5.3 has since **merged its harness into `esp32-server` main (`63bacb61`)** while
still BLOCKED — deliberately, and explicitly not as a Done claim — on the grounds that the tooling
is reusable and `seed-sim-backend.sql` "was already lost once sitting untracked". Re-verified at
close of this session: `robot/` is still not a git repo, and T6.1's two scope files are still
tracked nowhere (`git ls-files` in `esp32-server` matches neither `lesson_studio_task14_soak` nor
`lesson-memory-soak`), so the N/A above stands as written. But the campaign's working practice for
untracked `robot/` files is now *relocate into `esp32-server` and merge*, not *wait for F-T53-03*.
Whoever unblocks T6.1 should consider moving the soak driver there rather than leaving a
multi-hour harness untracked — the same failure mode F-T53-03 exists to prevent.

Steps 1, 3, 4 are moot: nothing was changed, so there is nothing to re-verify, deploy, or re-test.

## Findings routing

**No new §5 rows added.** Both causes are already logged and OPEN:

- **F-T53-09** (blocks the lesson path) — `LESSON_PRODUCTION_PLAN.md` §5, owner T1.3 / T5.3
- **F-T53-03** (robot/ untracked, blocks the ship path) — §5, owner T0.4 / T7.5

Adding rows would duplicate them. The observation in §2 above (verify command cannot fail) is
about T6.1's own scope, so it is recorded here rather than in the cross-task findings log.

Pre-existing inbound row for this task, still OPEN and **not** addressed (it needs a soak window
that cannot run yet): the 2026-08-06 T3.3 row routed to T6.1 — `tbot-mobile`
`ParentHistoryScreen.tsx:40-54` renders the entire accumulated history into a `ScrollView` with no
virtualization, so each "Load more lessons" grows the mounted view tree without bound.

## Unblocking condition

T6.1 becomes runnable when T5.3 reaches DONE — concretely, when F-T53-09 is resolved (seed a
second curriculum lesson into the studio fixture, or add a generation-rebuild mode that tolerates
a demo-only corpus) so an assignment can reach `ASSET_PACK_READY` and a lesson can run to
completion. Boxes 1, 2 and 5 all unlock together at that point. Box 3 and the ingest half of box 4
could be run earlier if a partial soak is ever wanted.

Full shipping of T6.1's scope-file changes additionally needs F-T53-03 (make `robot/` a tracked
repo), otherwise the branch/gate/worktree protocol stays N/A.

## Environment at gate-check time

Docker stacks up and healthy: `tbot-ls-e2e-{esp,web,backend,pg,mysql,redis,seed-pg,seed-mysql}`,
plus the long-running `tbot-esp32-server{,-web,-db,-redis}`. The blocker is not infrastructure
availability — the stack is up and the device connects; the assign step is the wall.
