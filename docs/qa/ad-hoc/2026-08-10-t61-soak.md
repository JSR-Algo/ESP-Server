# T6.1 soak and memory evidence - 2026-08-10

## Current evidence state

`RUN_-5_STATUS: PASS`

The definitive run `-5` completed with `status=PASS` in
`/Users/manhhodinh/Documents/TBOT/robot/evidence/t61-soak-final-2026-08-10-5/report.json`.
Every `done_checks` value is `true`. No plan or task status is changed by this evidence note.

## Repro

F-T61-01 reproduced as a manager API socket leak during WebSocket churn:

- Restarted `tbot-ls-e2e-esp`.
- Baseline sample: 120,792 KiB RSS / 17 threads / 22 file descriptors.
- Ran 20/20 WebSocket connect/disconnect cycles.
- Waited 30 seconds for async cleanup.
- Follow-up sample: 136,016 KiB RSS / 17 threads / 51 file descriptors.
- Later sample still showed 51 file descriptors and 22 established connections to
  `172.24.0.7:8002`.

F-T61-02 reproduced as a fixture limit gap: the T5.3 simulation ESP service did not set
`LESSON_SD_MAX_FILE_BYTES`, so the real materialize endpoint failed closed with
`503 INVALID_LIMIT`.

## Fix diff

- `ManageApiClient` now uses request-scoped `httpx.AsyncClient` ownership for normal async
  requests and closes the client in the request path. `safe_close()` remains compatible with
  legacy cached clients and singleton reset.
- The T5.3 simulation compose now sets the ESP service SD file limit to the production-bounded
  value.
- The soak support fixes include token-cache reservation, monotonic elapsed-time accounting, SD
  limit handling, and restart-aware resource-series analysis.
- The attempted terminal-tail analyzer relaxation was reverted. Keeping the strict analyzer avoids
  masking staircase leaks that only appear at lesson-tail boundaries.
- Tests covered manager-client closure, SD limit/compose coverage, token-cache reserve behavior,
  monotonic resource analysis, and the T6.1 driver/parser self-tests.

## Reruns

| Run | Result | Evidence |
| --- | --- | --- |
| Full run `-4` | NOT PASS | Only the cold lesson allocator slope check was red. Source: `/Users/manhhodinh/Documents/TBOT/robot/evidence/t61-soak-final-2026-08-10-4/report.json`. |
| Warm diagnostic after `-4` | DIAGNOSTIC FLAT | Lesson-resource-only short run, overall `NOT_PASS`, with `idle_seconds=0`, `ws_churn=0`, `sd_cycles=0`, and `generation_requests=0`; not a full Done-gate run. Within that diagnostic, 20 lesson samples stayed flat at 161,056 -> 161,060 KiB, the steady-state proof after the cold allocator slope failure. Source: `/Users/manhhodinh/Documents/TBOT/robot/evidence/t61-warm-followup-2026-08-10/report.json`. |
| Definitive run `-5` | PASS | Final release-sized run is green: all done checks true, no restart, 20 lessons, 3,600-second idle, WS100, SD10, generation, ingest, backend resources, and worker resources passed. |

## Run `-5` metrics

| Gate | Result | Evidence |
| --- | --- | --- |
| Done checks | PASS | `backend_generation_bounded`, `backend_ingest_sustained`, `backend_latency_stable`, `backend_resources_bounded`, `generation_worker_resources_bounded`, `idle_resources_flat`, `idle_window_elapsed`, `lesson_resources_flat`, `n_consecutive_lessons`, `no_container_restart`, `sd_pack_cycles_return_to_baseline`, and `ws_churn_no_leak` were all `true`. |
| Lessons | PASS | 20 lessons; RSS 161,060 -> 161,064 KiB; back-half slope 0.181818 KiB/sample; FDs 19 -> 19; threads 17 -> 17. |
| Idle | PASS | Elapsed 3,600.124784 seconds; RSS 164,220 -> 165,508 KiB; back-half slope 0.0; FDs 19 -> 19; threads 17 -> 17. |
| WebSocket churn | PASS | 100/100 completed; latency p95 0.081743 seconds, max 0.084261 seconds; resource metrics RSS 161,064 -> 164,216 KiB, back-half slope 0.0, FDs 19 -> 19, threads 17 -> 17. |
| SD cycles | PASS | 10/10 cycles `ok=true`; each restored the 283 KiB byte-exact snapshot and evicted back to the 0 KiB baseline. |
| Backend load | PASS | Generation drained after 20 queued requests; generation request p95 early 0.020198 seconds, late 0.012244 seconds, overall 0.015681 seconds; ingest advanced 4,638 -> 5,278 events (+640). |
| Lesson latency | PASS | p95 early 92.978748 seconds, late 96.415112 seconds, overall 95.011961 seconds. |
| Backend resources | PASS | RSS 211,046 -> 211,968 KiB; FDs 0 -> 0; threads 18 -> 18. |
| Generation worker resources | PASS | RSS 146,330 -> 127,386 KiB; FDs 0 -> 0; threads 18 -> 18. |
| Restart guard | PASS | One ESP container identity; restart count remained 0. |

## Ship checklist

1. **Re-verify at tip: PASS.** The branch was rebased onto local `main` at `84a2798a`.
   The literal task command failed with exit 127 because this host has no `python` shim;
   `python3 scripts/lesson_studio_task14_soak.py --help` passed. Driver/parser verification
   passed (`41` parser tests plus both self-tests), the focused ESP regression set passed
   `45` tests, and the repository suite passed `3,792` tests with `3` skipped in the declared
   `.venv311` environment. A system-Python run initially failed two Google Live tests because
   that interpreter lacked the repository-declared `google-genai` package; the same failures
   reproduced on pre-merge `main`, and both passed in `.venv311` without source changes.
2. **T0.4 and merge: PASS.** `gate.sh t61` proved RED at `84a2798a` and GREEN at branch tip
   `8fd744a8`. `merge-task.sh` repeated the gate and created merge `01f8b9d9` on local `main`
   as campaign merge `#62`.
3. **Deploy: DEFERRED.** T6.1 changes ESP runtime code and the simulation compose, but the
   standing campaign decision batches ESP/VPS deployment in T7.3. No backend deployable code
   changed, and no deployment was performed in this task.
4. **Re-test on main: PASS.** `verify-on-main.sh` tested merge `01f8b9d9` in throwaway
   worktrees. The `python3` help command passed, the T6.1 repro passed both behavior checks,
   the focused ESP set passed `45` tests, and the full ESP suite passed `3,789` tests with
   `6` skipped. The literal `python` command again returned 127 solely because the host has no
   `python` executable.
5. **Cleanup: PASS.** The task branch was confirmed merged, the worktree was clean, the
   registered worktree and local branch were removed, and no remote task branch existed. A
   leftover `.DS_Store` was removed so the worktree directory could be deleted. The earlier
   untracked blocked evidence draft was preserved at
   `/Users/manhhodinh/Documents/TBOT/robot/evidence/t61-soak-canonical-blocked-draft-2026-08-10.md`
   instead of being discarded.
6. **Close out: PASS.** `lesson-prod/t61-soak.md` and `LESSON_PRODUCTION_PLAN.md` section 2
   both record T6.1 as DONE with this evidence link. The release-sized soak passed every Done
   check and all Ship steps are closed.
